# Release

Stone Set uses a single private two-user release path.

Start with [`PRIVATE_RELEASE.md`](PRIVATE_RELEASE.md).

Preferred repeat build command on Windows:

```powershell
.\tool\release\private-release.ps1
```

Do not add staging, Play Store publishing or release infrastructure unless the product scope changes beyond the two known users.
