import 'package:flutter/widgets.dart';

/// Temporary layout values for foundation placeholders only.
///
/// These values are not the Stone Set product design system.
abstract final class StoneSetFoundationTokens {
  static const contentPadding = EdgeInsets.all(24);
  static const contentMaxWidth = 480.0;
  static const textGap = 8.0;
}
